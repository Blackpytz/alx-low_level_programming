#include "main.h"

/**
 * print_rev - prints a string in reverse
 * @s: Argument
 *
 * Return: Always 0.
 */

void print_rev(char *s)
{
	int x = 0;

	while ('\0' => s[x])
	{
		putchar(*s);
		s--;
	}
	putchar('\n');
}
