#include "main.h"

/**
* add - function that adds two integers and returns the result
* @a: first integer
* @b: second integer
*
* Return: the result
*/
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
