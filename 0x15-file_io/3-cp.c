#include "main.h"

void copy_text(const char *file from, char *file_to);

/**
* main - main function
* @ac: argument count
* @av: argument vector
*
* Return: Always 0.
*/
int main(int ac, char **av)
{
	if (ac != 3)
	{
		dprinf(2, "Usage: cp file_from file_to");
		exit(97);
	}
	copy 
}
